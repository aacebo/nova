name: test
on: [push]

vars:
  greeting: hello
  count: 3

templates:
  banner: "=== {{ greeting }} ==="

steps:
  - name: hello
    run: greeting

  - if: count > 0
    name: check_count
    run: "'count was positive'"
