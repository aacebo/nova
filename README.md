# Nova

## Backlog

- HTTP Module
    - `hello {{ http.get('...') }}`
- File IO Builtins
    - `{{ fs.read('...') }}`
- Codec Module
    - `{{ json.parse('...') }}` 
    - `{{ json.stringify(o) }}` 
    - `{{ yaml.parse('...') }}`
    - `{{ yaml.stringify(o) }}`
- AI Module
    - `my name is {{ ai.complete('my name is') }}`
